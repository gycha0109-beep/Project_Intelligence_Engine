# PIE Stage 10I — Validation

## Focused regression

Final local Stage 10I focused suite:

```text
16 / 16 PASS
```

Coverage includes:

- exact Stage 10A source replay intake,
- exact 40-hex git revision enforcement,
- duplicate task/revision inflation rejection,
- identical intake idempotence,
- capture-before-report temporal rejection,
- `WORKFLOW_ACCEPTED` exclusion,
- `REVIEWED` projection,
- controlled-evaluation SAFE reconciliation,
- controlled-evaluation idempotence,
- unsupported Outcome authority rejection before mutation,
- semantic threshold-forgery detection,
- immutable snapshot replay,
- snapshot mutation detection,
- symlink source rejection,
- registry/manifest rollback on partial replace failure,
- standalone and `pie-trust` CLI delegation.

Before the final organizational module/test split, the integrated Stage 10G + Stage 10H + Stage 10I focused set completed:

```text
46 / 46 PASS
```

The final split changed module/test organization, not the contracts; the Stage 10I focused suite was rerun after that split.

## Initial GitHub full regression

Initial implementation HEAD:

```text
8b04809502969c40b6cd60934eb9908a2a05eca0
```

CI:

```text
#922
run 32109148344
```

Matrix result:

```text
Python 3.11 SUCCESS
Python 3.13 SUCCESS
Python 3.14 SUCCESS
```

Each job completed:

- editable install,
- package asset synchronization,
- full unittest discovery,
- existing `urs` version/profile/Finding validation,
- wheel build.

## Final validation requirement

After these Stage 10I documents and architecture index are committed, the documentation-inclusive exact HEAD must pass the same Python 3.11/3.13/3.14 matrix. That final run is the terminal implementation authority.

Root and packaged copies of `r0-prospective-evidence-campaign-report.schema.json` must remain byte-identical.

## Runtime interpretation

Implementation PASS is not evidence-threshold PASS.

The current real prospective campaign remains:

```text
assessment_count=0
reviewed_count=0
conclusive_outcome_count=0
independent_audit_count=0
pilot_authorized=false
automation_authorized=false
```

Actual future cases must be collected over time. No test fixture or historical workflow action may be substituted into the runtime campaign.
