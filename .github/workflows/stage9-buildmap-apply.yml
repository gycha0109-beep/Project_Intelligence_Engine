name: Stage 9 BuildMap Apply

on:
  push:
    branches:
      - agent/stage-9-buildmap-export
    paths:
      - scripts/apply_stage9_buildmap.py
      - .github/workflows/stage9-buildmap-apply.yml

permissions:
  contents: write

concurrency:
  group: stage9-buildmap-apply
  cancel-in-progress: true

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/stage-9-buildmap-export
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Apply and diagnose
        run: |
          set +e
          python scripts/apply_stage9_buildmap.py > stage9-hardening.log 2>&1
          apply=$?
          echo "APPLY_EXIT=$apply" >> stage9-hardening.log
          python scripts/sync_package_assets.py >> stage9-hardening.log 2>&1
          sync=$?
          echo "SYNC_EXIT=$sync" >> stage9-hardening.log
          pip install -e . >> stage9-hardening.log 2>&1
          install=$?
          echo "INSTALL_EXIT=$install" >> stage9-hardening.log
          python -m compileall -q src tests >> stage9-hardening.log 2>&1
          compile=$?
          echo "COMPILE_EXIT=$compile" >> stage9-hardening.log
          python -m unittest discover -s tests -p 'test_buildmap_export*.py' -v >> stage9-hardening.log 2>&1
          focused=$?
          echo "FOCUSED_EXIT=$focused" >> stage9-hardening.log
          python -m unittest discover -s tests -v >> stage9-hardening.log 2>&1
          full=$?
          echo "FULL_EXIT=$full" >> stage9-hardening.log
          echo "RESULT=$apply,$sync,$install,$compile,$focused,$full" >> stage9-hardening.log
          exit 0
      - name: Commit diagnosis and verified files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add stage9-hardening.log src/review_system/buildmap_export.py tests/test_buildmap_export.py tests/test_buildmap_export_hardening.py schemas/buildmap-export.schema.json src/review_system/assets/schemas/buildmap-export.schema.json src/review_system/cli.py pyproject.toml
          git diff --cached --quiet || git commit -m "capture Stage 9 relationship hardening diagnosis"
          git push origin HEAD:agent/stage-9-buildmap-export
