name: Validation - BuildMap PR67 corroboration feasibility

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/validation-buildmap-pr67-corroboration-feasibility.yml'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

ajobs:
